A very simple utility, written in python that will center the mouse cursor in any given window based on only the window title.

Usage:
- mouse_recenter.exe "WINDOW TITLE"

To get a list of current window titles:
- mouse_recenter --list